{
  "name": "battleships-grad-project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext .js,.jsx,.ts,.tsx",
    "format": "prettier --write .",
    "sass:watch": "sass --update src:dist --watch",
    "sass:build": "sass --no-source-map --style=compressed src:dist"
  },
  "devDependencies": {
    "eslint": "^10.0.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.5",
    "prettier": "^3.8.1",
    "sass": "^1.97.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "robust-websocket": "^1.0.0"
  }
}
